{% extends 'base.html.twig' %}

{% block title %}Edit Username{% endblock %}

{% block body %}
    <h1>Modifier le pseudo</h1>
    <div>
    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
	</div>
    <div>
    <a href="{{ path('user_show', {'id': user.id}) }}" class=>Annuler et retourner au profil</a>
	</div>
{% endblock %}
