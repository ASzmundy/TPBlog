{{ form_start(form) }}
    {{ form_widget(form) }}
    <div style="padding-bottom:60px">
    <a href="{{ path('categorie_new') }}">
        Créer une catégorie
    </a>
	</div>
    <div>
    <button class="btn btn-lg btn-primary">{{ button_label|default('Envoyer') }}</button>
    </div>
{{ form_end(form) }}